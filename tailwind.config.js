/** Tailwind config for the Graham & Doddsville dashboard.
 *  Built with the lab standalone CLI (see README "Rebuilding Tailwind CSS").
 *  Design tokens per plans/00-STANDARDS.md §5: indigo-600 primary,
 *  slate-50/900 base, default sans stack (no custom webfonts).
 */
module.exports = {
  content: ["./app/templates/**/*.html"],
  theme: {
    extend: {},
  },
  plugins: [],
};
